{% extends 'base.html.twig' %}

{% block title %}Hello ListeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{{ include('base.html.twig') }}
    <h1>Listes</h1>

    <table class="table">
        <thead>
            <tr>
              
                <th>Nom</th>
                <th>niveau</th>
               <th>action</th>
            </tr>
        </thead>
        <tbody>
       
            {% for listess in listess %}
                <tr>
                    
                    <td>{{ listess.nom }}</td>
                    <td>{{ listess.niveau }}</td>
                    <td>
                        <form action="{{ path('matieres_etudiant') }}" method="get">
    
                        <div class="form-group form-check">
                        
                            <input type="hidden" class="form-check-input" name="classe" value='{{ listess.niveau }}'>
                            <input type="hidden" class="form-check-input" name="nom" value='{{ listess.nom }}'>
                        
                        
                        </div>
                        <button type="submit" class="btn btn-primary">matiere</button>
                        </form>
                        <a href="{{ path('user_show', {'id': listess.id}) }}">show</a>
                        {{ include('user/_delete_form.html.twig') }}
                        
                  </td>
                
                </tr>
               
            {% else %}
                <tr>
                    <td colspan="5">no records found</td>
                </tr>
            {% endfor %}
        
        </tbody>
    </table>
 
{% endblock %}
